const {EmbedBuilder}    = require('discord.js');
const config            = require('../../config.json');

module.exports = {
    name: 'template',
    description: 'Template command file',
    execute(message,args){

        // Do stuff here

    }
}


